import * as operacoes from './ex02-operacores-arrays.mjs';

const numeros = [1, 2, 3, 4, 5, 6, 7, 8, 9];

console.log(operacoes.filtrarPares(numeros));
console.log(operacoes.dobrarValores(numeros));
console.log(operacoes.somarValores(numeros));